apiVersion: apps/v1
kind: Deployment
metadata:
  name: integration-service
  namespace: oms
  labels:
    app: integration-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: integration-service
  template:
    metadata:
      labels:
        app: integration-service
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: integration-service
          image: registry.example.com/oms/integration-service:v0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8086
              name: http
          envFrom:
            - configMapRef:
                name: oms-config
            - secretRef:
                name: oms-secret
          env:
            - name: SERVER_PORT
              value: "8086"
          resources:
            requests:
              cpu: 200m
              memory: 384Mi
            limits:
              cpu: "1"
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8086
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8086
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: integration-service
  namespace: oms
spec:
  selector:
    app: integration-service
  ports:
    - port: 8086
      targetPort: 8086
      name: http
