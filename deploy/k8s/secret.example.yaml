# 生产密钥示例：请勿直接提交真实值。
# 推荐使用 External Secrets Operator / Sealed Secrets / 云厂商 Secret Manager 注入。
# kubectl create secret generic oms-secret -n oms \
#   --from-literal=OMS_JWT_SECRET='<强随机密钥>' \
#   --from-literal=MYSQL_PASSWORD='<数据库密码>' \
#   --from-literal=ALIPAY_APP_PRIVATE_KEY='<渠道私钥>'
apiVersion: v1
kind: Secret
metadata:
  name: oms-secret
  namespace: oms
type: Opaque
stringData:
  OMS_JWT_SECRET: "CHANGE-ME-STRONG-RANDOM-SECRET"
  MYSQL_PASSWORD: "CHANGE-ME"
